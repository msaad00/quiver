# Security grades

> Auto-generated by `scripts/regen_security_grades.py` at **2026-06-24 03:52 UTC**. Do not hand-edit. CI regenerates and `--check`s for drift weekly + on dependency changes.

## Composite

**Grade: A**  (score: 99 / 100)

Weighted across four scanner rows. Weights:
- 30% Bandit (code-level findings, severity LOW+)
- 30% In-repo validators (skill contract, framework coverage, etc.)
- 20% pip-audit (dependency CVEs)
- 20% agent-bom skills-scan credentials axis

## Row summary

| Row | Scanner | Result | Grade |
|---|---|---|---|
| Code-level findings | `bandit -r skills mcp-server scripts runners` (severity LOW+) | HIGH=0 · MEDIUM=0 · LOW=58 over 72,597 LOC | A |
| Trust-contract validators | 14 in-repo `validate_*.py` gates | 14/14 pass | A |
| Dependency CVEs | `pip-audit` against resolved deps | 0 finding(s) | A |
| Skill credential hygiene | `agent-bom skills scan` credentials axis | 0 leakage signals across 286 files | A |

## Bandit — code-level findings

- Scanned: 72,597 lines of code
- **HIGH severity: 0**
- **MEDIUM severity: 0**
- **LOW severity: 58** (informational; most are subprocess / exception-handler patterns expected in a security-skills repo)

Top test IDs by count:
  - `B603`: 17
  - `B105`: 17
  - `B404`: 14
  - `B110`: 5
  - `B112`: 4
  - `B607`: 1

## In-repo validators — trust contract

14 of 14 passing. Each one is a CI gate; drift fails closed.

| Validator | Status |
|---|---|
| `validate_skill_contract.py` | pass |
| `validate_skill_count_consistency.py` | pass |
| `validate_skill_structure.py` | pass |
| `validate_skill_runtime.py` | pass |
| `validate_safe_skill_bar.py` | pass |
| `validate_dependency_consistency.py` | pass |
| `validate_framework_coverage.py` | pass |
| `validate_ocsf_metadata.py` | pass |
| `validate_captured_provenance.py` | pass |
| `validate_skill_integrity.py` | pass |
| `validate_presets.py` | pass |
| `validate_deny_list_parity.py` | pass |
| `validate_golden_ocsf.py` | pass |
| `validate_test_coverage.py` | pass |

## pip-audit — dependency CVEs

No known CVEs in the resolved dependency tree.

## agent-bom — skill trust + provenance

- Files scanned: **286**
- Credential env vars referenced (count, not leaked): **38**
- Packages found: **8** · MCP servers found: **1**

### Trust categories (level distribution per file × category axis)

| Category | pass | info | warn | fail |
|---|---:|---:|---:|---:|
| credentials | 239 | 47 | 0 | 0 |
| purpose_capability | 0 | 132 | 154 | 0 |
| instruction_scope | 0 | 0 | 279 | 7 |
| install_mechanism | 0 | 99 | 0 | 187 |
| persistence_privilege | 0 | 132 | 154 | 0 |

### Provenance status

- **unsigned**: 286

**Interpretation.** A high `unsigned` provenance count is a known gap — sigstore bundle signing for skill bundles is roadmap, not shipped. Verdict labels like `malicious` in agent-bom default heuristics map to 'agent-bom couldn't verify provenance or policy metadata' — they are not assertions of actual malice. The credentials axis (0 leaks) and the in-repo validators are the load-bearing signals.

**Trust frontmatter coverage:** 131/131 shipped skills declare `purpose`, `capability`, `persistence`, `telemetry`, `privilege_escalation`. The remaining warn/fail rows are scanner heuristics for unsigned provenance, instruction-scope strictness, and install-mechanism expectations; they are not a leaked-secret signal.

## Methodology

Grades are mechanical. Each scanner row is graded on raw counts (see `_grade()` in `scripts/regen_security_grades.py`). The composite weights are tuned so Bandit and the in-repo validators (the most actionable signals) dominate, while agent-bom's heuristic verdicts are informational. Composite ≥ 95 is the bar for an unqualified A; anything below B is a release-blocker.

## Regeneration

```bash
uv run python scripts/regen_security_grades.py            # write
uv run python scripts/regen_security_grades.py --check    # CI gate
```
