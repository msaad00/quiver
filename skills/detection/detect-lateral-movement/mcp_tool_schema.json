{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "detect-lateral-movement MCP parameters",
  "description": "Typed parameters for lateral-movement detection over merged API + network activity.",
  "type": "object",
  "properties": {
    "input_path": {
      "type": "string",
      "description": "Path to merged native or OCSF JSONL input. Omit to read stdin via the input field.",
      "minLength": 1,
      "x-cli-style": "positional"
    },
    "output_path": {
      "type": "string",
      "description": "Write Detection Finding JSONL to this path instead of stdout.",
      "minLength": 1,
      "x-cli-flag": "--output"
    }
  },
  "examples": [
    {
      "input": "{\"class_uid\":3001}\n",
      "output_format": "ocsf"
    },
    {
      "input_path": "/tmp/merged-events.jsonl",
      "output_path": "/tmp/lateral-findings.jsonl"
    }
  ]
}
